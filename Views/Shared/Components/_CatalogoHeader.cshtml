@model dynamic
@{
    var titulo = ViewBag.Titulo ?? "Configuración";
    var descripcion = ViewBag.Descripcion ?? "Administre la información del sistema";
    var icono = ViewBag.Icono ?? "ri-settings-line";
    var breadcrumb = ViewBag.Breadcrumb ?? "Configuración";
}

<!-- Page Header -->
<div class="page-title-box d-sm-flex align-items-center justify-content-between">
    <h4 class="mb-sm-0">@titulo</h4>
    <div class="page-title-right">
        <ol class="breadcrumb m-0">
            <li class="breadcrumb-item"><a href="javascript: void(0);">Highdmin</a></li>
            <li class="breadcrumb-item active">@breadcrumb</li>
        </ol>
    </div>
</div>

<!-- Info Card -->
<div class="card mb-4" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white;">
    <div class="card-body">
        <div class="d-flex align-items-center">
            <div class="flex-shrink-0">
                <div class="avatar-sm">
                    <span class="avatar-title bg-white bg-opacity-20 text-white rounded-circle fs-3">
                        <i class="@icono"></i>
                    </span>
                </div>
            </div>
            <div class="flex-grow-1 ms-3">
                <h5 class="mb-1 text-white">@titulo</h5>
                <p class="text-white-50 mb-0">@descripcion</p>
            </div>
        </div>
    </div>
</div>