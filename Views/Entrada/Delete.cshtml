@model EntradaItemViewModel
@{
    ViewData["Title"] = "Eliminar Entrada";
    Layout = "~/Views/Shared/_VerticalLayout.cshtml";
}

<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0">Eliminar Entrada</h4>
            <div class="page-title-right">
                <ol class="breadcrumb m-0">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index")">Entradas</a></li>
                    <li class="breadcrumb-item active">Eliminar</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header bg-danger-subtle">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <div class="avatar-sm">
                            <span class="avatar-title bg-danger-subtle text-danger rounded-circle fs-5">
                                <i class="ri-delete-bin-line"></i>
                            </span>
                        </div>
                    </div>
                    <div class="flex-grow-1 ms-3">
                        <h5 class="card-title mb-1 text-danger">Confirmar Eliminación</h5>
                        <p class="text-muted mb-0">¿Está seguro que desea eliminar esta entrada?</p>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="alert alert-warning" role="alert">
                    <i class="ri-alert-line me-2"></i>
                    <strong>¡Advertencia!</strong> Esta acción no se puede deshacer. Se eliminará permanentemente la entrada del sistema.
                </div>

                <div class="table-responsive">
                    <table class="table table-borderless mb-0">
                        <tbody>
                            <tr>
                                <th class="ps-0" scope="row">ID:</th>
                                <td class="text-muted">#@Model.Id.ToString("D6")</td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Insumo:</th>
                                <td class="text-muted">@Model.InsumoNombre (@Model.InsumoCodigo)</td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Tipo:</th>
                                <td><span class="@Model.TipoBadgeClass">@Model.InsumoTipo</span></td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Fecha:</th>
                                <td class="text-muted">@Model.FechaEntrada.ToString("dd/MM/yyyy")</td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Cantidad:</th>
                                <td class="text-muted fw-semibold">@Model.Cantidad.ToString("N0") unidades</td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Usuario:</th>
                                <td class="text-muted">@Model.UsuarioNombre</td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Mes:</th>
                                <td class="text-muted">@Model.Mes</td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Notas:</th>
                                <td class="text-muted">@(Model.Notas ?? "Sin observaciones")</td>
                            </tr>
                            <tr>
                                <th class="ps-0" scope="row">Estado:</th>
                                <td><span class="@Model.EstadoClass">@Model.EstadoTexto</span></td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <div class="text-end mt-4">
                    <a href="@Url.Action("Index")" class="btn btn-secondary me-2">
                        <i class="ri-arrow-left-line me-1"></i> Cancelar
                    </a>
                    <form asp-action="Delete" method="post" class="d-inline">
                        <button type="submit" class="btn btn-danger">
                            <i class="ri-delete-bin-line me-1"></i> Confirmar Eliminación
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>